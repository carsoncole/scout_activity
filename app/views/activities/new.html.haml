
%h1 What's your Activity idea?


= image_tag "feature-activity-icons.png", class: 'img-fluid rounded shadow-sm p-0 mb-5'

- if signed_in?
  = render 'form'

- else
  .mt-4= button_to "Sign in to propose an activity", sign_in_path, method: :get, class: 'btn btn-primary'