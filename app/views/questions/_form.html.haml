= form_for [@troop, @activity, @question] do |f|
  - if @activity.errors.any?
    #error_explanation
      %h2= "#{pluralize(@question.errors.count, "error")} prohibited this question from being saved:"
      %ul
        - @question.errors.full_messages.each do |message|
          %li= message

  .form-group
    = f.label :question, 'What is your question on this activity?'
    = f.text_field :content, class: 'form-control', placeholder: 'Enter your question'


  .action
    = f.submit "Ask your question", class: 'btn btn-primary'